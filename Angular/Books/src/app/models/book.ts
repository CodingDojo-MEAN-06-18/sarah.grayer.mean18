export class Book { //create class here w/.ts file, import it into component.ts. Give the shape of the class info here
    id: number;
    title: string;
    author: string;
    pages: number;
    year: number;
    publisher: string;
} 